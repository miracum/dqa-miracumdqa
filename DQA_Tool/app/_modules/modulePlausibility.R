# (c) 2019 Lorenz Kapsner
# moduleVisualizationsServer
modulePlausibilityServer <- function(input, output, session, rv, input_re){
  
}


modulePlausibilityUI <- function(id){
  ns <- NS(id)
  
  tagList(
    box(title="Plausibility Checks")
  )
}
